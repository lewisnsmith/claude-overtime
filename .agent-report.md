1. Rewrote `commands/overtime.md` as a single unified command with flag dispatch covering all 7 invocation patterns.
2. Consolidated single-task (from v1 overtime.md) and backlog (from all-nighter.md) flows; preserved all 10 session rules + 3 backlog-specific rules (11–13) + customRules append.
3. Backlog mode uses `lib/rate-limit.js` for auto delay, cursor-based state for mid-cycle resume, and parallel track isolation via worktrees.
4. Cleanup is fully trap'd in single mode and handled in both exit paths (clean + paused) in backlog mode; settings.local.json backup/restore preserved throughout.
5. Deleted `commands/all-nighter.md` via `git rm`; the `--resume` flag and paused-exit banner replace the v1 resume pattern.
