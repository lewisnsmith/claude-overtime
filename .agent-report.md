## B1 Agent Report

1. Rewrote `bin/claude-overtime.js` to delegate config to `lib/config.js` and state to `lib/state.js`; added `install --dry-run`, `state show/reset`, `config validate`, and full `uninstall` with active-session guard.
2. `install` registers Stop, SessionStart, and PreToolUse hooks + status line into `~/.claude/settings.json`; idempotent — no duplicate entries on re-run.
3. Removed `postinstall` script from `package.json`; bumped version to `2.0.0`.
4. Added `tests/bin/cli.test.js` with 13 tests covering dry-run, idempotency, install+uninstall, state show, config get/set/validate — all pass.
5. No lib or hook files were modified; all work confined to `bin/` and `package.json` per scope.
