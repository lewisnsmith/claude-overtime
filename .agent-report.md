## B3: Status Line + Onboarding — Agent Report

Rewrote `hooks/overtime-statusline.sh` to read from `~/.claude/overtime-statusline-cache.json` (written by `stop.sh`) instead of parsing stdin or calling Node.
Uses `jq` with `python3` fallback; zero Node dependency.
Implements 20-char `=`-filled progress bar, native (`72%`) vs fallback (`~72%`) tilde prefix, and one-time onboarding banner gated by `~/.claude/overtime-onboarded` marker file.
Added `tests/hooks/test-statusline.sh` with 14 test cases (isolated tmp HOME); all pass via `bash tests/hooks/test-statusline.sh`.
No other files modified.
