## A4 Hook Scripts — Agent Report

Delivered `hooks/stop.sh`, `hooks/session-start.sh`, `hooks/pre-tool-use.sh` (all chmod 755) and `tests/hooks/test-hooks.sh`; all 21 tests pass with `bash tests/hooks/test-hooks.sh`.
`stop.sh` uses jq-or-python3 JSON parsing (zero Node dependency); native rate-limit path reads `.rate_limits.five_hour.*`; fallback accumulates token counts against 200k window; warn banner fires once per session via `/tmp/overtime-warned-<CLAUDE_SESSION_ID>`.
`session-start.sh` GCs stale state on every boot: dead PID (`kill -0` fails) or `expires_at < now` → restore `settingsBackup` to `<projectRoot>/.claude/settings.local.json` (or delete if null), best-effort kill caffeinate/sleep children, log to `~/.claude/overtime-cleanup.log`.
`pre-tool-use.sh` is inert when `~/.claude/overtime-state/` is empty; when active it blocks `rm -rf /`, `rm -rf ~`, `git reset --hard`, `git push --force/-f`, `git push` to `main`/`master`/protectedBranches, and `git push` to any ref not matching `overtime/*` or `v2/*` (v2 allowed for migration period); Edit/Write blocks outside project root and against deny globs (`node_modules/**`, `.git/**`, `**/.env*`).
No `lib/` modules touched; no `bin/` touched; `v2/*` push allowance is intentional for the migration wave and noted here per spec.
