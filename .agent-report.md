# Agent A1 Report — lib/config.js

**Built:** `lib/config.js` (centralized config loader) and `tests/lib/config.test.js` (20 tests, all passing).

**Schema:** Matches Section 4 exactly — new fields `delayBuffer`, `autoOvertime`, `editAllowGlobs`, `editDenyGlobs`; `warnAt` is now 0-100 percentage (was raw token count); `defaultDelay` defaults to `"auto"`.

**Deviations from plan:** `GLOBAL_CONFIG_PATH` is implemented as a dynamic getter (not a static string) so `os.homedir()` is evaluated at call time — required for both test isolation and correct runtime behavior in environments that change HOME. The exported value is still string-typed at read time; external callers work normally.

**Files changed:** `lib/config.js` (new), `tests/lib/config.test.js` (new). No other files modified.

**Test status:** `node tests/lib/config.test.js` → 20 passed, 0 failed.
