# Agent C1 Report — Wave 3 Docs

## Discrepancies found (code vs plan)

1. **Wave 1+2 code not present in worktree.** `lib/`, `hooks/session-start.sh`, `hooks/pre-tool-use.sh`, `hooks/stop.sh` do not exist. Only `hooks/rate-limit-warn.sh` and `hooks/overtime-statusline.sh` are present. The worktree appears to branch from main (v1.4), not from a merged `v2/integration` branch.

2. **`bin/claude-overtime.js` is v1.4.** Still references `all-nighter`, `postinstall`, `/tmp/claude-overtime-state.json`, `warnAt` as a token count (`90000`), `defaultDelay: "5h"`, and has no `state` subcommand. The plan's v2 CLI changes from Wave 2 (B1) are absent.

3. **`commands/overtime.md` is v1.** No flag dispatcher for `--backlog`, `--resume`, or `--auto`. The v2 flag matrix documented in the plan has not been implemented.

4. **Status line prefix mismatch.** The existing `overtime-statusline.sh` outputs `OT: 72%` (prefix `OT:`). The plan specifies `overtime: 72% [...]`. README documents the plan's format; if using the current hook the prefix will differ.

5. **`package.json` still has `postinstall` and version `1.4.0`.** The plan's headline change (removing `postinstall`) has not landed. README correctly documents the v2 behavior (no postinstall auto-run) but the package still auto-runs install on `npm install -g`.
