{
  "name": "claude-overtime",
  "version": "0.1.0",
  "description": "A Claude Code plugin that enables unattended, rate-limit-resilient coding sessions via the /overtime slash command.",
  "author": "Lewis Smith",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/lewisnsmith/claude-overtime.git"
  },
  "keywords": [
    "claude",
    "claude-code",
    "plugin",
    "automation",
    "rate-limit",
    "caffeinate"
  ],
  "bin": {
    "overtime": ".claude/skills/overtime/scripts/overtime-daemon.sh"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
